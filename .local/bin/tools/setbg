#!/usr/bin/env bash

[ -z "$1" ] && exit

file="$1"
ext=${file##*.}

rm ~/.config/wall.*
cp "$1" ~/.config/wall.$ext && notify-send -i "$HOME/.config/wall.$ext" "Change wallpaper."
feh --bg-scale ~/.config/wall.$ext
